apiVersion: v1
kind: Service
metadata:
  name: app
spec:
  type: NodePort
  replicas: 1
  ports:
  - name: http
    port: 8888
    protocol: TCP
    targetPort: 8888
    NodePort: 8888
    podIPs:
      - ip: 127.0.0.1
  selector:
    app: app
